package com.example.yousc;

import org.junit.Test;
import static org.junit.Assert.*;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class CommentFetchTest {

    /*  IN PROGRESS */
    @Test
    public void testCommentFetchFormat() {
        CommentsActivity test = new CommentsActivity();
        List<Comment> testList = new ArrayList<>();
//        test.handleCommentFetch();
//        boolean validParameters = test.checkTextFields(testEmail, testPassword);
//        assertFalse("Valid, non-empty parameters must be inputted", validParameters);
    }
}
